#pragma once
#include <Camera.h>
#include <Object.h>

class Scene{
    private:
        //Lights ?
        //Objects ?
        //Camera ?
        std::vector<Ball> objects;
        Camera camera;
};